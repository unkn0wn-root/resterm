# CLI Reference

Resterm can execute request files, serve mocks, import collections, and inspect history without opening the TUI.

```text
resterm run requests.http
resterm mock requests.http
resterm record --upstream https://service.example.com --out captured.http
resterm history stats
```

Run `resterm --help` for available commands or `:docs cli` for the complete CLI manual.
